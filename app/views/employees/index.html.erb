<h2 class="m-5 text-center">
  Lista de Empleados (<%= @company.employees.count %>)
</h2>

<div class="d-flex justify-content-end">
  <%= form_tag company_employees_path(@company), method: :get, class: "form-inline" do %>
    <div class="input-group">
      <%= text_field_tag :search, params[:search], class: "form-control", placeholder: "Buscar por nombre o rol" %>
      <div class="input-group-append">
        <%= submit_tag "Buscar", class: "btn btn-primary" %>
      </div>
    </div>
  <% end %>
</div>


<table class="text-center m-5 mx-auto">
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Edad</th>
      <th>Cédula</th>
      <th>Cargo</th>
      <th>Departamento</th>
      <th>Acciones</th>
    </tr>
  </thead>
  <tbody>
    <%= render @employees %>
  </tbody>
</table>
