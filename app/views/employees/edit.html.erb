
<div class="container">
  <div class="row justify-content-center">
    <div class="col-md-6">
      <%= simple_form_for @employee, url: company_employee_path(@employee.company, @employee) do |f| %>
        <h2 class="m-5 text-center"><strong>Editar Información del Empleado</strong></h2>
        <div class="form-group">
          <%= f.input :avatar, as: :file, label: "Subir Avatar", input_html: { class: "form-control" } %>
        </div>
        <div class="form-group mt-3">
          <%= f.submit "Guardar cambios", class: "btn btn-primary" %>
        </div>
      <% end %>
    </div>
  </div>
</div>
